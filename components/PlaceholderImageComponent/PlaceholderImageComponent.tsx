import styles from "./PlaceholderImageComponent.module.scss"

export default function PlaceholderImageComponent() {
    return (
        <div className={styles["container-section-placeholder-image"]}>
        </div>
    )
}